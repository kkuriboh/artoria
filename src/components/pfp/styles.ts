import styled from 'styled-components/native'
import Bg from '../../assets/pfp_bg.svg'

export const Container = styled.View`
	justify-content: center;
	align-items: center;
`

export const Background = styled(Bg)`
	position: absolute;
	top: 0;
	left: 0;
`
